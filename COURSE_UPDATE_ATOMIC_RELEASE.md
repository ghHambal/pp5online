# แก้การบันทึกคอร์สของครู

ติดตั้ง `patch_course_update_atomic.sql` ใน Supabase SQL Editor **ก่อนเผยแพร่ frontend**
SQL นี้ต้องรันด้วยเจ้าของฐานข้อมูลตามวิธีติดตั้ง migration ปกติ
ติดตั้ง SQL บน production วันที่ 14 กันยายน 2569 แล้วผ่าน Supabase SQL Editor
ตรวจการเรียกฟังก์ชันด้วยสิทธิ์ authenticated ของครูรหัส 1023 สำเร็จ
โดยบันทึกค่าเดิมและ ROLLBACK ข้อมูลทดสอบทั้งหมด (ดู `tests/course-update-production.sql`)
ยังไม่ได้ยืนยันการกดบันทึกจากหน้าจอครูหลังเผยแพร่

หน้าครูใช้ `update_subject_atomic` แทนการแก้รายวิชา ลบครูร่วมสอน แล้วเพิ่มใหม่เป็นหลายคำขอ
RPC ตรวจสิทธิ์จาก `auth.uid()` และข้อมูลจริงในฐานข้อมูล จากนั้นบันทึกใน transaction เดียว
ถ้าบันทึกรายชื่อไม่สำเร็จ ข้อมูลวิชาจะย้อนกลับด้วย ไม่แจ้งสำเร็จเมื่อไม่ได้แก้ข้อมูล
การตรวจสิทธิ์ใน RPC ไม่เรียก policy ของตารางซ้ำ จึงรองรับกรณี policy วนซ้ำตามภาพแจ้งปัญหา
ไม่ได้ปิด RLS หรือเปลี่ยนนโยบายของตารางอื่น และไม่ได้เปลี่ยนเส้นทางบันทึกหน้าแอดมิน

- เจ้าของวิชาและแอดมินแก้ข้อมูลวิชาและรายชื่อครูร่วมสอนได้
- ครูร่วมสอนแก้ข้อมูลวิชาได้ แต่เปลี่ยนเจ้าของวิชาหรือรายชื่อครูร่วมสอนไม่ได้
- ครูเปลี่ยนเจ้าของวิชาไม่ได้; ต้องให้แอดมินดำเนินการ
- บุคคลอื่น/ผู้ไม่เข้าสู่ระบบถูกปฏิเสธ
- ฟอร์มหยุดหากโหลดครูร่วมสอนไม่สำเร็จ ป้องกันรายชื่อเดิมถูกแทนด้วยรายการว่าง

ทดสอบในเครื่อง:

```sh
python3 tests/run-course-update-atomic.py
node tests/course-update-client.mjs
```

SQL test สร้าง PostgreSQL ชั่วคราวผ่าน Unix socket ทดสอบ policy ที่ตั้งให้วนซ้ำจริง,
เจ้าของวิชา, ครูร่วมสอน, ครูอื่น, แอดมิน, การเปลี่ยนเจ้าของโดยไม่ได้รับอนุญาต,
รายชื่อซ้ำ, การรักษารายชื่อเดิม, การปิดสอนร่วม และ rollback เมื่อครูปลายทางไม่มีอยู่จริง

หลังติดตั้งให้ตรวจด้วยบัญชีครูจริง: แก้ชื่อ/รหัส/ชั้นปี บันทึก เปิดซ้ำตรวจค่า
แล้วทดสอบเพิ่ม/ลบครูร่วมสอนด้วยเจ้าของวิชา และตรวจว่าครูอื่นแก้คอร์สไม่ได้
การผ่านในฐานจำลองไม่ใช่การยืนยันว่าฐานจริงไม่มี trigger หรือ constraint เพิ่มเติมที่กระทบการบันทึก
