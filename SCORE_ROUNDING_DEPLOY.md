# การปัดเลขร่วมและคะแนนพิเศษ

สถานะเผยแพร่ v10.22.700: ยังไม่ได้ติดตั้ง SQL ใน production เนื่องจาก Supabase แสดง `Resizing project` และช่องทาง CLI ติดข้อจำกัด IPv6 หน้าตั้งค่าจะแจ้งข้อผิดพลาดเมื่อบันทึกค่าร่วมไม่ได้ ส่วน ปพ.5 ใช้รูปแบบเริ่มต้นพร้อมคำเตือน ไม่ควรใช้เอกสารจริงจนตรวจค่าร่วมสำเร็จ

1. ติดตั้ง `patch_class_score_display_settings.sql` ใน Supabase SQL Editor ก่อนเผยแพร่ frontend โดยต้องมี `has_class_access` และ `get_user_role` จากระบบสิทธิ์เดิม
2. หน้าครู → คะแนน → ตั้งค่าปัดเลข → เลือกคอลัมน์ → **บันทึกใช้ร่วมกัน**
3. รีโหลดหน้าคะแนนนักเรียนและสร้าง ปพ.5 ใหม่ ค่าจากฐานข้อมูลจะใช้แทนค่าประจำเบราว์เซอร์

ค่าปัดเลขเดิมในเครื่องครูยังเป็นค่าเฉพาะเครื่องจนกดบันทึกใช้ร่วมกัน การปัดเลขเปลี่ยนเฉพาะการแสดงผล ไม่แก้ `student_scores` และเกรดคำนวณจากคะแนนจริงเทียบคะแนนเต็ม

คะแนน `bonus` และคะแนนพิเศษแบบเดิมแสดงแยกให้นักเรียน ไม่บวกเข้ายอดรวมหรือเกรด คอลัมน์ `override` เป็นเครื่องมือครู ไม่แสดงเป็นคะแนนเพิ่ม สูตรโบนัสที่ครูเชื่อมกับคอลัมน์หลักไว้โดยเจตนายังคงทำงาน

ตรวจหลังติดตั้ง: ครู/ผู้ร่วมสอนบันทึกค่าได้ นักเรียนในวิชาอ่านได้แต่เขียนไม่ได้ ผู้ไม่เกี่ยวข้องอ่านหรือเขียนไม่ได้ ทดสอบคะแนนทศนิยม/ศูนย์/ช่องว่าง และคะแนนพิเศษในคณิตศาสตร์พื้นฐาน ม.5/2 เปรียบเทียบทั้งสามหน้า โดยไม่แก้คะแนนจริงเพื่อทดสอบ
