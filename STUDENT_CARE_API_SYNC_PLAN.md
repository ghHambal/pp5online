# แผน: ซิงก์ข้อมูลผ่าน API จากระบบดูแลนักเรียน

สถานะ: **ยังไม่เริ่มสร้าง** — รอลิงก์ API จากโปรแกรมเมอร์ที่ดูแลระบบดูแลนักเรียน
บันทึกไว้เมื่อ: 2026-09-08

## บริบท

ปัจจุบัน pp5-online ซิงก์รายชื่อนักเรียนจาก Google Sheet (ดู `PROJECT_NOTES.md`, `gas/pp5-sync.gs`, `js/sync.js`) ซึ่งจริงๆ แล้วต้นทางของ Google Sheet นั้นคือ "ระบบดูแลนักเรียน" ที่มีโปรแกรมเมอร์อีกคนดูแลอยู่เบื้องหลัง

ภาคเรียนที่ 2/2569 ระบบดูแลนักเรียนจะมีข้อมูลเพิ่มจากเดิม:
- คอร์สวิชา (course catalog)
- รายวิชาที่ครูแต่ละคนสอน (teacher ↔ subject assignment)
- ตารางสอน (schedule)
- ครูที่ปรึกษา/ครูที่ปรึกษาศาสนาประจำห้อง (homeroom advisor assignment) — เปลี่ยนได้ทุกเทอมเหมือนกัน
  ธรรมชาติเดียวกับ 3 ข้อบน (บันทึกไว้ 2026-09-08: เดิมไม่ได้อยู่ในสโคปนี้ แต่ปุ่ม "ขึ้นภาคเรียนใหม่"
  ของ pp5-online เองก็ไม่ได้แตะตาราง `homeroom_teachers` เลย ทำให้ทุกเทอมต้องมอบหมายใหม่ด้วยมือ
  ทั้งหมด — ถ้า API มีข้อมูลนี้ด้วยก็ควรใช้ prefill ให้ เพราะเป้าหมายคือให้ "ขึ้นภาคเรียนใหม่"
  เทียบเท่ากับ "เปิดเทอมใหม่" จริงๆ ไม่ใช่แค่เปลี่ยนเลขปี/เทอมอย่างเดียว)
  **หมายเหตุ (2026-09-08):** ข้อมูลชุดนี้ระบบดูแลนักเรียนอาจไม่มีเก็บไว้ก็ได้ — ไม่ใช่ hard
  dependency เหมือน 3 ข้อบน หน้าจัดการที่ปรึกษาแบบกรอกมือของ pp5-online เอง (`renderHomeroom`,
  เมนู "homeroom" ในแดชบอร์ดแอดมิน, ผ่าน `assignHomeroomTeacher`/`deleteHomeroomTeacher` ใน
  `js/api.js`) **ต้องใช้งานได้ตามปกติเสมอไม่ว่า API จะมีข้อมูลนี้หรือไม่** ถ้า API มีให้ก็ใช้เป็น
  prefill เสริมได้ ถ้าไม่มีก็ไม่กระทบอะไร แอดมินกรอกเองตามเดิม

และสามารถขอ **API ที่ครอบคลุมข้อมูลทั้งหมดนี้ได้** (ไม่ใช่แค่รายชื่อนักเรียนเหมือนตอนนี้)

## เป้าหมาย

1. ซิงก์รายชื่อนักเรียนผ่าน API โดยตรง แทนการอ่าน Google Sheet
2. เตรียมข้อมูลคอร์สวิชา/รายวิชาที่สอน/ตารางสอน/ครูที่ปรึกษาให้ล่วงหน้าจาก API เมื่อขึ้นเทอมใหม่ แทนที่ต้องกรอก/มอบหมายเองทั้งหมดด้วยมือ — เป้าหมายคือ "ขึ้นภาคเรียนใหม่" ในระบบเทียบเท่ากับการเปิดเทอมใหม่จริงๆ

## แนวทางที่ตกลงกันไว้แล้ว

### 1) รายชื่อนักเรียน — สลับต้นทางข้อมูล (การเปลี่ยนแปลงจำกัดวง)

- จุดที่เปลี่ยน: ขั้นตอน "อ่านข้อมูลต้นทาง" ใน `gas/pp5-sync.gs` (`_syncStudentsFromSheet`) จากอ่าน Google Sheet เปลี่ยนเป็นเรียก API แทน
- logic ที่เหลือ (upsert เข้า Supabase `students`, log ลง `student_sync_logs`, auto-enroll ตาม `main_room`/`religion_room`) ใช้ของเดิมได้เกือบทั้งหมด ไม่ต้องออกแบบใหม่
- ข้อเสนอเพิ่มเติม: ย้ายตัวรัน sync จาก Google Apps Script ไปเป็น **Supabase Edge Function** แทน เพราะ GAS ปัจจุบันฝั่งแอปเรียกผ่าน JSONP/`fetch(mode:'no-cors')` ซึ่ง**ยืนยันผลสำเร็จ/ล้มเหลวจริงจากฝั่งเซิร์ฟเวอร์ไม่ได้** — ถ้าเปลี่ยนมาเรียก API ภายนอกอยู่แล้ว ใช้โอกาสนี้ย้ายไปทำใน Edge Function จะได้ auth header/error handling ที่ถูกต้องและตรวจสอบผลได้จริง

### 2) คอร์สวิชา / ครูผู้สอน / ตารางสอน / ครูที่ปรึกษา — ข้อมูลใหม่ทั้งหมด ต้องออกแบบเพิ่ม

ข้อมูลชุดนี้ **ไม่ใช่แค่ต่อยอด sync นักเรียน** เพราะปัจจุบันครูเป็นคนเปิดคอร์ส/ลงทะเบียนรายวิชาเองในระบบ (ดู Phase 2 — Teacher Portal Workflow) และแอดมินเป็นคนมอบหมายครูที่ปรึกษาเองด้วยมือทุกเทอม ถ้า API เข้ามาแทนที่ข้อมูลส่วนนี้ ต้องตัดสินใจว่าจะให้มันเป็น "แหล่งความจริง" แทนที่การกรอกมือทั้งหมด หรือแค่ช่วยเตรียมข้อมูลให้

**มติที่ตกลงแล้ว:** ใช้แนวทาง **"เตรียมข้อมูลไว้ล่วงหน้า (prefill) แล้วให้ครูยืนยันตอนล็อกอิน"**
- เมื่อครูล็อกอินเข้าระบบ ข้อมูลคอร์ส/รายวิชา/ตารางสอนของครูคนนั้นที่ได้จาก API จะถูกเตรียมไว้ให้แล้ว
- ครูกดยืนยันเพื่อสร้างเป็นห้องเรียนจริงในระบบ
- หลังยืนยันแล้ว ครูยังสามารถเพิ่ม/แก้ไข/ลบเองได้ตามปกติ — **ไม่ใช่การซิงก์ทับอัตโนมัติแบบเงียบๆ**
- ครูที่ปรึกษา/ครูที่ปรึกษาศาสนา: ยังไม่ได้ตกลงรูปแบบยืนยันชัดเจน (ให้แอดมินยืนยันแทนที่ครูแต่ละคน หรือให้ครูที่ถูกมอบหมายยืนยันเอง) — รอออกแบบพร้อมกับส่วนอื่นตอนได้ API จริง

## สิ่งที่ต้องรู้จาก API ก่อนเริ่มออกแบบจริง

- วิธียืนยันตัวตน (API key / OAuth / อื่นๆ)
- รูปแบบการดึงข้อมูล: pull (เรียกดึงเองตามรอบ) หรือ push/webhook (ระบบดูแลนักเรียนยิงมาหาเราเมื่อมีการเปลี่ยนแปลง)
- โครงสร้างข้อมูลจริงของคอร์ส/รายวิชา/ตารางสอน — มีรหัสวิชา/รหัสครู/รหัสห้องที่ map เข้ากับ schema ปัจจุบันของ pp5-online ได้อย่างไร (เช่น `students.student_code`, `teachers`, `classes`, `courses`)
- rate limit / ความถี่ที่ดึงข้อมูลได้

## ขั้นตอนถัดไป

1. ผู้ใช้ (ครูฮัมบาลีย์) คุยกับโปรแกรมเมอร์ที่ดูแลระบบดูแลนักเรียน ขอลิงก์ API + รายละเอียดข้างต้น
2. เมื่อได้ข้อมูล API มาแล้ว ค่อยออกแบบ schema/flow จริงสำหรับส่วนคอร์ส/ครูผู้สอน/ตารางสอน (ยังไม่มีในระบบตอนนี้)
3. ยังไม่เริ่มเขียนโค้ดใดๆ จนกว่าจะได้รายละเอียด API จริง
